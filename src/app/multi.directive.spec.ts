import { MultiDirective } from './multi.directive';

describe('MultiDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiDirective();
    expect(directive).toBeTruthy();
  });
});
